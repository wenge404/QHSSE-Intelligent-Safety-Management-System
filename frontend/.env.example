# Copy to .env.local. Never commit .env.local.

# The Express API. NEXT_PUBLIC_ prefixed variables are inlined into the client
# bundle at build time, so this must not hold anything secret — it is a public
# origin, which is exactly what it is.
NEXT_PUBLIC_API_URL="http://localhost:4000"
